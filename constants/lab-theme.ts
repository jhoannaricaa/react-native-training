// Shared constant, in its own module. Neither the screen nor the util owns it,
// so the screen file can stay a pure component module (a Fast Refresh boundary).
export const THEME = { accent: '#0a7ea4', radius: 8 };
